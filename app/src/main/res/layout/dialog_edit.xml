<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="viewmodel"
            type="me.worric.souvenarius.ui.detail.DetailViewModel" />
        <variable
            name="clickHandler"
            type="me.worric.souvenarius.ui.detail.EditDialog.EditTextClickHandler" />
        <variable
            name="textType"
            type="me.worric.souvenarius.ui.detail.DetailFragment.TextType" />
    </data>
    <ScrollView
        android:id="@+id/sv_detail_edit"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/btn_edit_save"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    style="@style/Widget.AppCompat.Button.Borderless.Colored"
                    android:onClick="@{clickHandler::onTextEdited}"
                    android:text="Save" />

                <Button
                    android:id="@+id/btn_edit_cancel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    style="@style/Widget.AppCompat.Button.Borderless.Colored"
                    android:onClick="@{clickHandler::onCancelled}"
                    android:text="Cancel" />

            </LinearLayout>

            <TextView
                android:id="@+id/tv_label_edit_detail"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text='@{"Edit the " + textType.name.toLowerCase}'
                android:labelFor="@+id/et_edit_detail" />

            <EditText
                android:id="@+id/et_edit_detail"
                android:layout_width="300dp"
                android:layout_height="wrap_content"
                android:imeOptions="actionDone"
                android:inputType="textMultiLine"
                android:text="@{viewmodel.currentSouvenir.getValueFromTextType(textType)}" />

        </LinearLayout>
    </ScrollView>
</layout>
